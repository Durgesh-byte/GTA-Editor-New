#include "AINCMNTemplateFuxns.h"

/*
#include <QHashIterator>

template<class KEY, class VALUE>
void AINCMNTemplateFuxns<KEY, VALUE>::clearMemory(QHash<KEY, VALUE *>& iHashOfData)
{
	QHashIterator<KEY, VALUE *> itr(iHashOfData);
	while (itr.hasNext())
	{
		itr.next();
		VALUE * pDATA = itr.value();
		if (pDATA)
		{
			delete pDATA;
			pDATA = NULL;
		}
	}
	iHashOfData.clear();
}*/

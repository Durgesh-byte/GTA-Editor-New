name: SonarCloud Scan

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  sonarcloud:
    name: SonarCloud Scan
    runs-on: windows-latest

    steps:
      # Step 1: Checkout the repository
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Fetch all history for SonarCloud to analyze changes

      # Step 2: Install curl using Chocolatey
      - name: Install curl
        run: |
          choco install curl -y
        shell: cmd
        env:
          chocolateyVersion: 2.3.0

      # Step 3: Install Qt 5.15.2 using install-qt-action
      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: '5.15.2'
          host: 'windows'
          target: 'desktop'
          arch: 'win64_msvc2019_64'
          dir: 'C:\Qt'
          install-deps: 'true'

      # Step 4: Set Qt environment variables
      - name: Set Qt environment variables
        run: |
          echo "INCLUDE=C:\Qt\Qt\5.15.2\msvc2019_64\include;C:\Qt\Qt\5.15.2\msvc2019_64\include\QtCore;%INCLUDE%" >> $env:GITHUB_ENV
          echo "LIB=C:\Qt\Qt\5.15.2\msvc2019_64\lib;%LIB%" >> $env:GITHUB_ENV
          echo "PATH=C:\Qt\Qt\5.15.2\msvc2019_64\bin;%PATH%" >> $env:GITHUB_ENV
        shell: pwsh

      # Step 5: Set up MSBuild (for Visual Studio build tools)
      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      # Step 6: Install SonarCloud build wrapper
      - name: Install SonarCloud build wrapper
        run: |
          mkdir sonar-build
          curl -o sonar-build/build-wrapper-win-x86.zip https://sonarcloud.io/static/cpp/build-wrapper-win-x86.zip
          if exist sonar-build\build-wrapper-win-x86.zip (echo Download successful) else (echo Download failed && exit /b 1)
          unzip sonar-build/build-wrapper-win-x86.zip -d sonar-build
          dir sonar-build
          dir sonar-build\build-wrapper-win-x86 2>nul || dir sonar-build\build-wrapper-win-x86-64 2>nul
          dir sonar-build\build-wrapper-win-x86\build-wrapper-win-x86-64.exe 2>nul || echo Build wrapper executable not found in build-wrapper-win-x86
        shell: cmd

      # Step 7: Restore NuGet packages (if your project uses NuGet)
      - name: Restore NuGet packages
        run: |
          msbuild GTAAPPLICATION.sln -t:restore
        shell: cmd

      # Step 8: Build the project with SonarCloud build wrapper
      - name: Build with SonarCloud build wrapper
        run: |
          sonar-build\build-wrapper-win-x86\build-wrapper-win-x86-64.exe --out-dir sonar-build msbuild GTAAPPLICATION.sln /t:Rebuild /p:Configuration=Debug /p:Platform=x64
        shell: cmd

      # Step 9: Run SonarCloud scanner
      - name: Run SonarCloud scanner
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          dotnet tool install --global dotnet-sonarscanner
          dotnet sonarscanner begin /k:"Durgesh-byte_GTA-Editor-New" /o:"durgesh-byte" /d:sonar.login="${{ secrets.SONAR_TOKEN }}" /d:sonar.host.url="https://sonarcloud.io" /d:sonar.cs.vscoveragexml.reportsPaths=coverage.xml
          dotnet sonarscanner end /d:sonar.login="${{ secrets.SONAR_TOKEN }}"
        shell: cmd
